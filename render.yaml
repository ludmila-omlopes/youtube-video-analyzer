services:
  - type: keyvalue
    name: youtube-analyzer-redis
    plan: starter
    ipAllowList: []
    maxmemoryPolicy: noeviction

  - type: web
    name: youtube-analyzer-mcp
    runtime: node
    plan: starter
    buildCommand: npm ci && npm run build
    startCommand: npm run start:http
    healthCheckPath: /healthz
    envVars:
      - key: REDIS_HOST
        fromService:
          type: keyvalue
          name: youtube-analyzer-redis
          property: host
      - key: REDIS_PORT
        fromService:
          type: keyvalue
          name: youtube-analyzer-redis
          property: port
      - key: GEMINI_API_KEY
        sync: false
      - key: YOUTUBE_API_KEY
        sync: false

  - type: worker
    name: youtube-analyzer-mcp-worker
    runtime: node
    plan: starter
    buildCommand: npm ci && npm run build
    startCommand: npm run start:worker
    envVars:
      - key: REDIS_HOST
        fromService:
          type: keyvalue
          name: youtube-analyzer-redis
          property: host
      - key: REDIS_PORT
        fromService:
          type: keyvalue
          name: youtube-analyzer-redis
          property: port
      - key: GEMINI_API_KEY
        sync: false
      - key: YOUTUBE_API_KEY
        sync: false

  - type: web
    name: youtube-analyzer-mcp-admin
    runtime: node
    plan: starter
    buildCommand: npm ci && npm run build
    startCommand: npm run start:admin
    healthCheckPath: /healthz
    envVars:
      - key: REDIS_URL
        fromService:
          type: keyvalue
          name: youtube-analyzer-redis
          property: connectionString
      - key: ADMIN_USERNAME
        sync: false
      - key: ADMIN_PASSWORD
        sync: false
      - key: BULL_BOARD_READ_ONLY
        value: "true"
